{
  "version": 1,
  "rules": [
    {
      "name": "block-git-no-verify",
      "command": "git",
      "block_args": ["--no-verify", "-n"],
      "reason": "Git hook bypass is forbidden. Hooks exist for a reason - fix the issue instead of bypassing safety checks."
    },
    {
      "name": "block-git-no-gpg-sign",
      "command": "git",
      "subcommand": "commit",
      "block_args": ["--no-gpg-sign"],
      "reason": "GPG signing bypass is forbidden. If you need to disable signing, configure it properly in git config."
    }
  ]
}
